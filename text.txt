extraction model 600 tokens were quite less adn the model kept stopping in the middle

KG performance is better, the answers are more concise, however it is much slower than the raw LLM

The performance difference was huge, from nearly 1 hour 10 mins down to 23 mins 

It was made faster by using the top k paragraphs in this case 5 (the latency can be further reduced by reducing the value of k at the slight expense of performance, I found that this was a good middle ground) with the help of cosine similarity and then creating the knowledge graph using these paragraphs with almost no affect in performance.

Since it is basically a knowledge graph creation this can be done on any domain as long as triples can be extracted

Cosine similarity is another mathematical tool that can be applied on any text. 

The combination of both provides a smaller knowledge graph which leads to less hallucinations and better and concise answers.

I chose LLM as a judge to check the performance of all the models and an Exact match algorithm to check how exact can the models get. 

These 4 models were chosen to display the differences between large and small models and the performance difference. Reasoning was also checked

Asyncio can be used to reduce the latency even further

One of the strengths and issues with this approach is since it reduces the size of the knowledge graph drastically, the gap between the nano and pro models shrinks. (the performance of nano models improves and the performance of pro models worsens due to less information)

